# @cos/web

The COS web app: club dashboards, activity feed, document hub.
Next.js; talks only to `services/api` over HTTP and WebSockets.

Not yet scaffolded.
First real code arrives with the v0.1 club page (see [roadmap](../../docs/ROADMAP.md)).
